---
"@autonomos/dashboard": minor
---

Org Chart rebuilt: a crashed manager keeps its team (shown as a ghost with Resume), agents waiting on you glow amber with a "N need you" strip, teams sit side by side with solo agents on an Unassigned shelf, cards show unread counts and recency like the sidebar, click opens an agent and right-click opens the same agent menu as the sidebar, and the Daylight theme renders correctly.
