---
"@autonomos/dashboard": patch
---

Sidebar status: "Ready" and "Idle" now show as one label, "Idle" (they meant the same thing, and Codex agents never showed "Ready"). On stale rows the Idle label fades with age alongside the timestamp; Needs input, Error, and working labels always stay full strength.
