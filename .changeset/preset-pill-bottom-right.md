---
"@autonomos/dashboard": patch
---

Reposition the env-preset pill to the bottom-right of sidebar agent rows with a gold-accent highlight (theme accent, not a provider brand color); long status labels now truncate instead of pushing the pill out of view, and the README hero stages a preset agent so the pill is visible.
