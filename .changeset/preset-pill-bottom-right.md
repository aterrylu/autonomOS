---
"@autonomos/dashboard": patch
---

Reposition the env-preset pill to sit immediately right of the repo·branch text on sidebar agent rows, with a gold-accent highlight (theme accent, not a provider brand color); long status labels now truncate instead of crowding the row, and the README hero stages a preset agent so the pill is visible.
