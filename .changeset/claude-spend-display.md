---
"@autonomos/server": patch
"@autonomos/dashboard": patch
---

feat(claude-usage): usage-based Enterprise accounts see their spend on the status bar instead of "no windows". Pick Text (default: "$620", amber "$930 / $1,000" near the limit), % or Bar in the usage panel. With no spend limit set, the total is shown. Spend is informational only and never pauses or resumes the usage queue. Team and other window plans are unchanged.
