---
"@autonomos/dashboard": patch
---

Projects: two different directories with the same name (e.g. `/tmp/aq/work` and `/tmp/ax/work`) no longer show as duplicate rows. Each is labelled by the fewest parent folders that tell them apart ("aq/work" / "ax/work"), and hovering a project shows its full path.
